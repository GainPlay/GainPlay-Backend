services:
  backend:
    build:
      context: .
    container_name: gainplay-backend
    ports:
      - "3000:3000"
    restart: unless-stopped
    extra_hosts:
      - "host.docker.internal:host-gateway"